name: CI API Test
on:
  # push:
  #   branches: [ 'snovitskyi/js_playwriight_POM' ]
  pull_request:
    branches: [ 'snovitskyi/rest' ]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-node@v3
    - name: Install dependencies
      run: npm ci

    - name: Run API tests
      run: npm run api_tests  


